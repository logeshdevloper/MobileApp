import 'package:flutter/material.dart';

// New Color Palette - Modern & Cohesive
const Color primaryColor =
    Color(0xFF5C6BC0); // Indigo - for headers and main UI elements
const Color secondaryColor =
    Color(0xFFFFA726); // Amber - for accents and highlights
const Color accentColor =
    Color(0xFFFFCC80); // Light Amber - for secondary accents
const Color highlightColor =
    Color(0xFFE8EAF6); // Light Indigo - for backgrounds and selections
const Color bgPrimary =
    Color(0xFFFFFFFF); // White - for cards and content areas
const Color bgSecondary =
    Color(0xFFF5F5F5); // Light Gray - for page backgrounds
const Color textPrimary =
    Color(0xFF263238); // Dark Blue Gray - for primary text
const Color textSecondary = Color(0xFF607D8B); // Blue Gray - for secondary text
const Color errorColor = Color(0xFFEF5350); // Red - for errors and warnings

// Product Card Colors
const Color productNameBg = Color(0xFF5C6BC0); // Indigo - matches primary color
const Color productPriceBg =
    Color(0xFFFFA726); // Amber - matches secondary color

// Legacy colors kept for backward compatibility
const Color lightBlue = Color(0xFFE6EFFF);
const Color darkBlue = Color(0xFF1E45A8);
const Color bgBlue = Color(0xFFF5F9FF);
const Color bgcard = Color.fromARGB(255, 198, 181, 246);
