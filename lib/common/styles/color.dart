import 'package:flutter/material.dart';

// Product Card Colors
const Color productNameBg = Color(0xFF1E45A8); // Indigo - matches primary color
const Color productPriceBg =
    Color(0xFFFFA726); // Amber - matches secondary color

// Legacy colors kept for backward compatibility
const Color lightBlue = Color(0xFFE6EFFF);
const Color darkBlue = Color(0xFF1E45A8);
const Color bgBlue = Color(0xFFF5F9FF);
const Color bgcard = Color.fromARGB(255, 198, 181, 246);
const Color bgcart = Color.fromARGB(255, 246, 243, 252);
// Colors from your preferred palette
const Color primaryColor = Color(0xFF008080); // Fresh Green
const Color secondaryColor = Color(0xFFFFA726); // Sunny Orange
const Color bgPrimary = Color(0xFFFFFFFF); // White
const Color bgSecondary = Color(0xFFF5F5F5); // Soft Gray
const Color textPrimary = Color(0xFF212121); // Charcoal
const Color textSecondary = Color(0xFF757575); // Muted Gray
const Color errorColor = Color(0xFFE53935); // Tomato Red
const Color highlightColor = Color(0xFFE8F5E9); // Light Green Highlight
